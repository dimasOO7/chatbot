## 🤖 PNIbot: Интеллектуальный Помощник для Малого Бизнеса

### Описание 💡

**PNIbot** — это чат-бот, разработанный специально для **помощи малому бизнесу** в широком спектре задач.

-----

### Ключевые Возможности 🚀

  * **Мультимодельный Подход (Model Routing):** Использование специализированных LLM для различных задач, что повышает эффективность и снижает затраты.
  * **RAG (Retrieval-Augmented Generation):** Возможность предоставлять контекст из загруженных пользователем файлов (PDF, DOCX, Excel/CSV) для получения ответов, основанных на частных данных бизнеса.
  * **Актуальная Информация:** Интеграция с асинхронным поиском (`aDDGS`) для предоставления ответов, основанных на свежих данных из Интернета.
  * **Безопасность:** Надежная система аутентификации с использованием **JWT** и хеширования паролей (**Argon2**).
  * **Оптимизация API:** Использование Open AI-совместимого API для легкой миграции на других провайдеров LLM.

**Демо-версия приложения доступна по ссылке:** [http://3537187-zu75095.twc1.net:8000/](http://3537187-zu75095.twc1.net:8000/)

-----

### ⚙️ Используемые Технологии и Инструменты

PNIbot построен на современном и асинхронном стеке, обеспечивающем высокую скорость работы и масштабируемость.

| Категория | Технология | Назначение |
| :--- | :--- | :--- |
| **Backend Framework** | **FastAPI** | Высокопроизводительный, асинхронный веб-фреймворк Python для создания API. |
| **Базы данных** | **aiosqlite** | Асинхронный драйвер для **SQLite**, используется для хранения истории чатов, данных пользователей и сессий. |
| **Фронтенд** | **HTML5, CSS3, JavaScript** | **SPA (Single Page Application)** для пользовательского интерфейса, логики авторизации, управления чатами и **обработки стриминга ответов**. |
| **Аутентификация** | **JWT (JSON Web Tokens)**, **Argon2** | Безопасная авторизация пользователей. **Argon2** используется для надежного **хеширования паролей**. |
| **Инструменты Поиска** | **aDDGS (DuckDuckGo Search)**, **aiohttp** | Асинхронный поиск **актуальной информации** в Интернете для RAG. |
| **Работа с Файлами (RAG)** | **pypdf**, **docx**, **pandas** | Извлечение текста из **PDF**, **DOCX** и **Excel/CSV** файлов для обогащения контекста LLM. |
| **LLM Совместимость** | **Open AI API (совместимый)** | Унифицированный доступ к LLM, обеспечивающий легкий переход на других поставщиков (например, Azure, Google). |

-----

### 🧠 Архитектура LLM (Model Routing)

Приложение использует API **Cerebras** и реализует принцип **Model Routing** (маршрутизации моделей). Это позволяет **разделять внутренние задачи** между специализированными моделями, уменьшая количество запросов к основной модели и оптимизируя использование бесплатного тарифа.

| Модель | Роль | Назначение |
| :--- | :--- | :--- |
| **gpt-oss-120b** | **Генератор Ответа** | **Основная модель.** Отвечает за формирование окончательного, подробного, структурированного ответа пользователю, используя всю предоставленную информацию (RAG-контекст, результаты поиска). |
| **llama-3.3-70b** | **Инструментальный Агент** | **Вспомогательная модель.** Используется для критических внутренних задач: **создание оптимальных поисковых запросов** (для `aDDGS`) и **фильтрация/валидация** ответов или входящих данных. |

> [\!CAUTION]
> **Географическое ограничение:** **Cerebras API не доступен в России**. Для успешной работы приложения при локальном развертывании **требуется использовать иностранный VPN или прокси**.

-----

### 🚀 Инструкция по Запуску

#### Вариант 1: Локальный Запуск с Uvicorn (Рекомендовано)

1.  **Клонируйте репозиторий и перейдите в папку проекта:**

    ```bash
    git clone https://github.com/dimasOO7/chatbot.git
    cd chatbot
    ```

2.  **Создайте и активируйте виртуальное окружение:**

    ```bash
    # Создание
    python -m venv venv
    # Активация (Linux/macOS)
    source venv/bin/activate
    # Активация (Windows PowerShell)
    .\venv\Scripts\Activate.ps1
    ```

3.  **Установите зависимости:**

    ```bash
    pip install -r requirements.txt
    ```

4.  **Создайте файл `.env`** в корневой папке и добавьте необходимые переменные, заменив значения на ваши ключи:

    ```ini
    CEREBRAS_API_KEY=cerebras-api-key_ваш_ключ
    JWT_SECRET_KEY=ваш_очень_сильный_и_случайный_секретный_ключ
    ```

5.  **Запустите приложение с автоматической перезагрузкой (`--reload`):**

    ```bash
    uvicorn app:app --reload
    ```

    Приложение будет доступно по адресу: **[http://127.0.0.1:8000](http://127.0.0.1:8000)**.

#### Вариант 2: Запуск с помощью Docker

1.  **Создайте файл `.env`** в корневой папке (см. Шаг 4 выше).

2.  **Соберите образ Docker:**

    ```bash
    docker build -t pnichat-app .
    ```

3.  **Запустите контейнер, пробросив порт:**

    ```bash
    docker run -d -p 8000:8000 --name pnichat pnichat-app
    ```

    Приложение будет доступно по адресу: **http://localhost:8000**.

-----

### ❓ Как узнать о возможностях бота?
Вы можете задать вопрос о его функциях и инструментах самому боту в чате после успешной авторизации.
