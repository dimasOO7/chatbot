# ----------------------------------------
# 1. Секретные и переменные окружения (Игнорируем)
# ----------------------------------------
# Мы передаем GEMINI_API_KEY через флаг -e при запуске, 
# или он должен быть в системе, где запускается Docker.
# .env часто содержит ключи, которые не должны попасть в образ.
#.env
pyenv.cfg

# ----------------------------------------
# 2. Файлы управления версиями (Игнорируем)
# ----------------------------------------
.git
.gitignore

# ----------------------------------------
# 3. Виртуальное окружение (Игнорируем)
# ----------------------------------------
# Мы устанавливаем зависимости внутри Dockerfile, 
# поэтому локальное окружение не нужно.
venv/
.venv/
include/
bin/
lib/
lib64/

# ----------------------------------------
# 4. Временные файлы и кэш (Игнорируем)
# ----------------------------------------
__pycache__/
*.pyc
*.swp
*.bak
*.tmp

# ----------------------------------------
# 5. Инструменты и IDE (Игнорируем)
# ----------------------------------------
.vscode/
.idea/
.DS_Store